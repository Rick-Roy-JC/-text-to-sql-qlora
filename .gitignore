__pycache__/
*.pyc
.venv/
venv/
.env

# Data (downloaded, not committed)
data/spider/
data/raw/
*.sqlite
*.db

# Model artifacts — adapters/ (final trained LoRA weights) are committed
# intentionally so reviewers can load them directly. Intermediate training
# checkpoints (optimizer/scheduler/scaler state) stay out — large, ephemeral,
# and already persisted to Drive during training.
checkpoints/

# Notebooks checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
